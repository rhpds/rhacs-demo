# VirtualMachine manifest for rhel-monitoring
# Prerequisites:
#   1. Create cloud-init secret first:
#      oc create secret generic cloudinit-monitoring --from-file=userdata=cloud-init-userdata-monitoring.yaml -n <namespace>
---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: rhel-monitoring
  namespace: default
  labels:
    app: rhacs-vm-scanning
    profile: monitoring
    roxagent: enabled
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        app: rhacs-vm-scanning
        profile: monitoring
        roxagent: enabled
        kubevirt.io/vm: rhel-monitoring
    spec:
      domain:
        cpu:
          cores: 2
        devices:
          autoattachVSOCK: true
          disks:
          - name: containerdisk
            disk:
              bus: virtio
          - name: cloudinitdisk
            disk:
              bus: virtio
          interfaces:
          - name: default
            masquerade: {}
        resources:
          requests:
            memory: 4Gi
      networks:
      - name: default
        pod: {}
      volumes:
      - name: containerdisk
        containerDisk:
          image: registry.redhat.io/rhel9/rhel-guest-image:latest
      - name: cloudinitdisk
        cloudInitNoCloud:
          secretRef:
            name: cloudinit-monitoring
