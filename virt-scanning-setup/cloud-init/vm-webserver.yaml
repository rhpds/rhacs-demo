# VirtualMachine manifest for rhel-webserver
# Prerequisites:
#   1. Create cloud-init secret first:
#      oc create secret generic cloudinit-webserver --from-file=userdata=cloud-init-userdata-webserver.yaml -n <namespace>
#   2. Ensure OpenShift Virtualization is installed with VSOCK enabled
#   3. Set namespace before applying (default: default)
---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: rhel-webserver
  namespace: default
  labels:
    app: rhacs-vm-scanning
    profile: webserver
    roxagent: enabled
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        app: rhacs-vm-scanning
        profile: webserver
        roxagent: enabled
        kubevirt.io/vm: rhel-webserver
    spec:
      domain:
        cpu:
          cores: 2
        devices:
          autoattachVSOCK: true
          disks:
          - name: containerdisk
            disk:
              bus: virtio
          - name: cloudinitdisk
            disk:
              bus: virtio
          interfaces:
          - name: default
            masquerade: {}
        resources:
          requests:
            memory: 4Gi
      networks:
      - name: default
        pod: {}
      volumes:
      - name: containerdisk
        containerDisk:
          image: registry.redhat.io/rhel9/rhel-guest-image:latest
      - name: cloudinitdisk
        cloudInitNoCloud:
          secretRef:
            name: cloudinit-webserver
