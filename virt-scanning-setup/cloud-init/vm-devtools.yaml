# VirtualMachine manifest for rhel-devtools
# Prerequisites:
#   1. Create cloud-init secret first:
#      oc create secret generic cloudinit-devtools --from-file=userdata=cloud-init-userdata-devtools.yaml -n <namespace>
---
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: rhel-devtools
  namespace: default
  labels:
    app: rhacs-vm-scanning
    profile: devtools
    roxagent: enabled
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        app: rhacs-vm-scanning
        profile: devtools
        roxagent: enabled
        kubevirt.io/vm: rhel-devtools
    spec:
      domain:
        cpu:
          cores: 2
        devices:
          autoattachVSOCK: true
          disks:
          - name: containerdisk
            disk:
              bus: virtio
          - name: cloudinitdisk
            disk:
              bus: virtio
          interfaces:
          - name: default
            masquerade: {}
        resources:
          requests:
            memory: 4Gi
      networks:
      - name: default
        pod: {}
      volumes:
      - name: containerdisk
        containerDisk:
          image: registry.redhat.io/rhel9/rhel-guest-image:latest
      - name: cloudinitdisk
        cloudInitNoCloud:
          secretRef:
            name: cloudinit-devtools
